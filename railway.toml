[build]
builder = "nixpacks"
watchPatterns = ["clients/paulo/**"]

[deploy]
startCommand = "node clients/paulo/src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
NODE_ENV = "production"
PORT = "8080"
